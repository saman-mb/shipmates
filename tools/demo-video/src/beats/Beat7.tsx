import React from 'react';
import { EndCard } from '../components/EndCard';

/**
 * Beat 7 — End card · 0:41–0:45, 4s hold, loop-friendly
 * (docs/DEMO_VIDEO.md §2). Copy and asset live in data/beats.ts BEAT7.
 */
export const Beat7: React.FC = () => {
  return <EndCard />;
};
